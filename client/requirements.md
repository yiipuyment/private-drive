## Packages
react-player | For embedding video players (YouTube, Twitch, etc.)
framer-motion | Smooth animations for UI elements
date-fns | Timestamp formatting for chat messages

## Notes
WebSocket at /ws
Real-time sync requires handling 'video-update' events carefully to avoid loops
Turkish language interface
